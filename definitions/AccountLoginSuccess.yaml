type: object
properties:
  profile:
    $ref: './Profile.yaml'
  pets:
    type: array
    items:
      $ref: './Pet.yaml'
  token:
    type: string
    description: 'See JWT.yaml for format'
  petTypes:
    type: array
    items:
      type: string
